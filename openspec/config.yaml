# SDD project configuration
# Generated by sdd-init on 2026-07-07.
project: mi-portafolio-web
artifact_store:
  default: engram
  fallback: openspec
execution_mode: interactive
review_budget_lines: 400
chain_strategy: auto-forecast
stack:
  framework: Astro
  language: TypeScript
  module_type: ESM
  package_manager:
    current: npm
    evidence:
      - package-lock.json present (lockfileVersion 3)
      - pnpm-lock.yaml absent
      - yarn.lock absent
      - bun lock absent
  app_type: static portfolio site
  source_paths:
    - src/pages
    - src/components
    - src/layouts
    - src/icons
commands:
  dev: npm run dev
  build: npm run build
  preview: npm run preview
  astro: npm run astro
testing:
  strict_tdd: false
  reason: No dedicated test runner or test script detected; build validation is available through `astro check && astro build`.
  validation_command: npm run build
  detected_scripts:
    build: astro check && astro build
phase_rules:
  sdd_apply:
    no_migration_without_spec: true
    prefer_incremental_commits: true
    require_apply_progress: true
  sdd_verify:
    require_build_validation: true
    command: npm run build
conventions:
  generated_artifacts_language: English
  conventional_commits: planned_for_tooling_migration
